# WireGuard Telegram Bot

Управляй своим WireGuard сервером через Telegram бот.

## Описание

Данный проект предназначен для упрощения развертывания и управления собственной VPN сетью на основе WireGuard.

### Возможности:

**Основные функции:**
- 🚀 Автоматическая установка и настройка WireGuard
- ➕ Создание клиентских конфигураций (по одному)
- 👥 **НОВОЕ: Массовое создание клиентов** из списка
- 🎯 Выбор IP адреса при создании клиента
- ➖ Удаление клиентских конфигураций по IP-адресу
- 📱 Генерация QR-кодов для мобильных устройств
- 💾 Создание резервных копий и восстановление (JSON файлы)
- 📤 Импорт конфигураций из файла
- 🔄 **Пересоздание конфигураций** на основе существующих файлов
- 🚫 Полное удаление WireGuard

**Мониторинг и статистика:**
- 📊 Подробная статистика сервера
- 👥 **Монитор клиентов** с отображением IP и имён
- 🗂 Улучшенный просмотр конфигураций с подробностями
- 📝 Логирование всех операций и обработка ошибок

## Настройка

### 1. Конфигурация окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
nano .env
```

Укажите:
- `TELEGRAM_BOT_TOKEN`: Токен вашего Telegram бота
- `AUTHORIZED_USERS`: ID пользователей через запятую
- `WG_LOCAL_IP_HINT`: Подсеть WireGuard (по умолчанию: 10.20.20)

### 2. Получение Telegram ID

Для получения своего Telegram ID используйте бот [@get_myidbot](https://t.me/get_myidbot)

### 3. Получение токена бота

1. Обратитесь к [@BotFather](https://t.me/botfather)
2. Создайте нового бота с помощью `/newbot`
3. Скопируйте полученный токен

## Установка и запуск

### Через Python

```bash
# Установка зависимостей
pip install --no-cache-dir -r requirements.txt

# Запуск бота
python3 main.py
```

### Через Docker

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## Логи

Логи сохраняются в файл `bot.log` и выводятся в консоль.

## Безопасность

- Бот поддерживает авторизацию по ID пользователей
- Все операции логируются
- Ошибки обрабатываются безопасно
- Пользовательский ввод санитизируется

## Подробное руководство

### 👥 Массовое создание клиентов

**Новая функция!** Создавайте множество клиентов одновременно, отправив список.

#### Доступ к функции:
1. Перейдите в раздел **Мониторинг**
2. Нажмите кнопку **Массовое_создание**

#### Поддерживаемые форматы списков:

**Формат 1 - Простой список (автоматический выбор IP):**
```
server1
server2
server3
laptop-john
phone-mary
```

**Формат 2 - С указанием IP адреса:**
```
server1:5
server2:10  
server3:15
laptop-john:20
phone-mary:25
```
*(где число после ':' - последний октет IP, например :5 = 10.20.20.5)*

**Формат 3 - Смешанный формат:**
```
server1
server2:10
server3
laptop-john:25
phone-mary
```

#### Возможности:
- ✅ Создание **неограниченного количества клиентов** за раз
- 🎯 Автоматический и ручной выбор IP адресов
- ⚡ Живой прогресс выполнения
- 📊 Подробная статистика создания
- 📦 Автоматическая отправка конфигураций:
  - До 5 клиентов - индивидуально
  - Более 5 клиентов - ZIP архив
- ✋ Валидация имен и IP адресов
- 🚫 Проверка дубликатов и конфликтов

#### Ограничения:
- Имена клиентов: только латинские буквы, цифры, дефисы, подчеркивания
- IP октеты: от 2 до 254 (1 зарезервирован для сервера)
- Максимум символов в имени: 50
- Количество клиентов за раз: без ограничений
- Строки начинающиеся с `#` игнорируются (комментарии)

### 🎯 Выбор IP при создании одного клиента

При создании клиента через **Добавить_конфиг**:
1. Введите имя клиента
2. Выберите IP из предложенных вариантов или нажмите **Автовыбор**
3. Конфиг создается с выбранным IP

### 💾 Резервное копирование и восстановление

#### Создание резервной копии:
1. **Администрирование** → **Сохранить_конигурацию**
2. Получите JSON файл с полной резервной копией:
   - Конфигурация сервера и ключи
   - Все клиентские конфигурации и ключи
   - Системные переменные
   - Временная метка создания

#### Восстановление из резервной копии:
1. **Администрирование** → **Импортировать_конфигурацию**
2. Отправьте JSON файл резервной копии
3. Подтвердите импорт (⚠️ **заменит текущую конфигурацию!**)
4. Автоматическое восстановление:
   - Создается локальная резервная копия
   - Останавливается WireGuard
   - Очищается текущая конфигурация
   - Восстанавливаются все файлы и ключи
   - Запускается WireGuard

### 🔄 Пересоздание конфигураций
Полезно после ручных настроек или восстановления:
- Сканирует `/etc/wireguard/` на предмет существующих клиентских конфигов
- Автоматически извлекает IP-адреса и имена клиентов
- Пересоздаёт файл `configs.txt` с актуальной информацией

### 👥 Монитор клиентов
Подробный просмотр всех подключенных клиентов:
- Полный список клиентов с IP-адресами
- Дата и время создания конфигурации
- Размер файлов конфигураций
- Свободные IP-адреса и диапазоны

### 📊 Расширенная статистика
- Статус сервера WireGuard
- Использование дискового пространства
- Время работы системы
- Последние созданные клиенты

## Требования

- Python 3.8+
- Права root на сервере
- Порт UDP для WireGuard (по умолчанию 51820)
- Доступ к скриптам в папке `scripts/`

## Лицензия

MIT License
